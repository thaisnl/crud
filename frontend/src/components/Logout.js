import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import axios from 'axios';

function Logout() {
    const [nome, setNome] = useState("")

    useEffect (()=> {
        //chamar o logout da api
    })

    return ( 
        <div></div>
        //renderizar um redirect p login 
    );
}

export default Logout;